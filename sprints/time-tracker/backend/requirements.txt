# Freelancer Time Tracker — Backend Dependencies
# Install with: pip install -r backend/requirements.txt

fastapi>=0.129.0
uvicorn[standard]>=0.40.0
python-multipart>=0.0.22   # Required for FastAPI form data (file uploads, etc.)
